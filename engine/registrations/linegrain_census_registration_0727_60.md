# REGISTRATION — line-grain census (findings v4), 2026-07-27, #60
*Registered BEFORE any run, per the run law. Convened by An in the #60 sitting —
her words of record: "this is illogical to fold … No I don't agree don't touch …
this thing needs to be archive[d] or deleted." The poem fold is retired
(SCORING_MANUAL §0.3-4 dated amendment, same day); this registration defines its
replacement. No new measurement occurs: every input is a committed, certificated
artifact — this run recomposes VERDICTS only.*

## What changes and what does not
- **DIES:** the line→poem max-precedence fold and every number resting on it
  (findings_v3 census: 298/290/62/225/24/23/8/7/6/5; the 82.1% rate; Table 1 v2).
- **LIVES untouched:** all measurement (line scalars, token deltas, booleans,
  organs, written/referent rows, type-prior charges), all credentials and cuts,
  all certificates, the 15-cell comparator, the per-word trigger law, the
  precedence rule AT line level (token/channel findings → one LINE state per
  field per side), fr/de/jp declared-loan 2-state, sonnet73 pilot exclusion,
  MT seats as controls, consensus-ghost lists (already line-level, 106 rows).

## Decision rule (the whole law)
Per (board, seat, field, line index i):
1. Source line-i state and seat line-i state are each composed by the EXISTING
   line-state law (per-word trigger → channel probes → precedence stated >
   latent > ghost > silent) from committed artifacts. Implementation of record
   for line states: the hunt's decision rule as already implemented in the
   exhibit path (`find_interesting_59.py` / `gen_sample_exhibits_59.py` lineage)
   and `rubric_compare.py`'s state machinery — reused, not reinvented.
2. Verdict(i) = the 15-cell crossing of the two line states.
3. **Alignment law:** crossings exist at index-aligned lines only (the
   consensus-list criterion, "index-alignment only"). Seats without index
   alignment (qingqing ×3, tiaotiao ×2, invitation ×1 per
   `consensus_ghost_boards_59.json` skip log) carry the honest STATUS
   "unaligned — awaits alignment file" in the verdict channel. Never dropped,
   never poem-folded. Line-count-mismatched tails at otherwise-aligned seats:
   absent-line markers, status not verdict (the F1 navigation caveat).
4. **Paired passes, as before:** once wording-only (stated/absent line states),
   once all-layers. The H2–H4 test restates at line grain: changed verdicts
   must fall in predicted cells.
5. **Grain declaration:** every output artifact carries `"grain": "line"` per
   crossing; poem-level anything appears nowhere. No grain speaks for another.

## Predictions staked (falsifiable, before the run)
- The latent-source row POPULATES: xibei L10/L11 sound = REVIVAL cells at the
  seats that state (CURATION C-flavor attests the split); the middle row of the
  comparator stops being structurally dark.
- The loom line L4 = RENDERED ×5 with Owen GHOST-CARRY at line grain (matching
  the existing line-level record).
- MT seats show line-grain UNHEARD > 0 (the eaten GT prediction's second half).
- ~~the PI's hypothesis, now testable: human seats also show UNHEARD > 0~~
  **DATED AMENDMENT, same sitting, pre-run — her catch:** wrong register. Human
  unhearing is not a hypothesis to stake — it is already VISIBLE in committed
  line-level artifacts (her words: "there are plenty human unhearing even in the
  'interesting' panels. stake what?" — e.g. E-flavor rank-divergence, the field
  moved house with seats sitting silent at a charged source line; A-flavor
  scrambles with silent seats). What v4 adds is the exact COUNTS and per-seat
  distribution, nothing more. The prediction register above is reserved for the
  genuine stakes (REVIVAL populating; the loom line reproducing).
- Survival-untouched has NO line-grain analogue staked: the wording-only vs
  all-layers comparison at line grain is reported as it falls.

## Certificates & hygiene
- No encoder pass occurs (states recompose from committed readings); if any
  step DOES require an encode, it inherits the batch-invariance certificate law
  (batch_size=1, seed-48 replay, drift < 1e-6, abort on failure).
- Inputs hash-checked against SCRIPT_MANIFEST/committed shas before composing.
- Outputs: `reports/findings_v4_linegrain_0727_60.{md,json}` + per-board
  crossing JSONs under `publishable/` per the tier-row law; findings_v3 moves
  to `archived/poem_fold_retired_0727/` in the same commit.
- Script: `caesitas_proto/linegrain_census_60.py`, sha recorded in
  SCRIPT_MANIFEST at landing. Aborts published, per the abort law.

## Chair-chosen defaults under standing law (flagged, not silent)
- Misaligned-seat handling = honest status (from "never drop a seat"), NOT a
  bespoke alignment attempt — alignment files remain a separate, unforced
  deliverable.
- sound_device stays a parallel organ channel at line grain (never
  trigger-gated), entering crossings exactly as the existing law composes it.
- Downstream paper artifacts (Table 1 v3, §4 rewrite, summary numbers,
  PROMPT_FOR_COWORK v2) are OUT OF SCOPE for this run — separate sitting work
  once v4 lands.
