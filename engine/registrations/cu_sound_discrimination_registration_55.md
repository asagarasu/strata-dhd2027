# CU DISCRIMINATION RUN — SOUND (#55.1, 2026-07-23 night; chair-declared, her veto window open and unused)

*The colour exam's design (cu_discrimination_registration_55.md, PASSED
+.171) ported to SOUND, verbatim in law. Registered before build; one go;
result stands. Purpose: the second discrimination credential — or the
finding that colour's pass was field-specific.*

## Population (all from committed records, frozen #54)
- **SUPPORTED**: the committed sound run's 21 validation positives (8
  fired at the committed record; charges REUSED verbatim under the
  standing reproduction warrant).
- **DISPUTED (hard negatives)**: the sound pool's covered_unsupported tier
  **FILTERED to covered==True members only** — the tier is mixed (chair
  discovery, 07-22: it also holds norm-uncovered words like 公交车); only
  words the zh3000 sensorimotor norm actually rated, with auditory
  support absent under the committed truth law (auditory-dominant ∨ CCFD
  rule), qualify as hard negatives (亮/低-class). The filter law and the
  resulting n publish at build; norm-uncovered members are listed as
  excluded-unknowns, never counted either way.
- **CONTROLS**: the committed sound control set, scored in-run per the
  unchanged null law.
- Machinery: the committed sound meter verbatim (sound module import; the
  sound axis; K=20, cap 32, seed 48, z ≥ 1.5, certificate; v5 exam-repair
  attestation laws as the sound run carried them).

## Endpoints (identical law to the colour exam; fixed pre-build)
PRIMARY: fire-rate(supported) − fire-rate(disputed), one-sided 95%
Newcombe lower bound > 0. SECONDARY (report-only, intervals): charge AUC ·
PPV at nomination prevalence · sensitivity column under the alternate
truth reading if one exists for sound (else declared n/a). Conservative
counting: every disputed fire counts against the meter.

## CHAIR'S PREDICTION (staked at registration, before build exists)
Sound's committed supported fire-rate is .381 (8/21) — weaker than
colour's .459, and the disputed set is smaller after the covered-True
filter (chair estimate: 12–20 usable hard negatives). **Predicted:
disputed fires 0–2; predicted verdict PASS but NARROWER than colour —
lower bound in +.05..+.20; P(PASS) ≈ .65** (the smaller n may leave the
bound under the line even with a real gap — an under-powered miss is the
likeliest failure shape, and it would read as "insufficient evidence,"
not "meter blind"). Surprise threshold: ≥5 disputed fires.

## What passing and failing mean (fixed now)
PASS → discrimination generalizes beyond colour; two fields carry the
credential. FAIL-by-margin (gap positive, bound ≤ 0) → publishes as an
effect estimate, no confirmatory claim, powered follow-up listed.
FAIL-by-rate (disputed fire like supported) → sound's latent-referent
claim retires; colour's pass stands alone and is reported as
field-specific. All three outcomes publish.

Registration commit precedes build; build (Opus agent, colour-exam script
as template, post-assertions on the run's own output: disputed-set
membership = the registered filter law; null n per the sound law) precedes
run; chair reviews before fire. Outputs:
`cu_sound_discrimination_55.py` + `results/cu_sound_discrimination_55.{json,md}`.
— #55.1
