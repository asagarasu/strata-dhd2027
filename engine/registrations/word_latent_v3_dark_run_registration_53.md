# Word-latent v3 DARK RUN — registration (#53, 2026-07-20; RUN ONLY ON HER GO)

*The DARK / illumination in-context (referent-instrument) run. Same machinery,
separate run, per the two-row frame (word_latent_v3_two_row_registration_53.md:
"Field: COLOR … Dark = immediate next, same machinery, separate run"). Shared
mechanism, floors, null design, K=20, cap 32, seed 48, certificate: all as
registered for color in word_latent_v2_incontext_registration_53.md — NOT
restated here. This document declares ONLY the dark deltas. Scorer:
word_latent_v2_incontext_dark_53.py (BUILD-ONLY; `--smoke` self-executable;
`main()` runs ONLY on her go).*

## Shared machinery (cited, unchanged from the color registration)
delta(s,w') = axis(embed(s)) − axis(embed(s[w→w'])); charge = median_hosts of
median_ensemble of delta; whole-sentence LaBSE; K=20 first-by-line hosts (n<3
FLAGGED-thin, 0 sits out); ensemble cap 32 (seeded word-stable sample,
random.Random("48:word")); null = control-role charges by the identical
procedure; z-floor 1.5; liveness gate prior≥0.35 (saturated-but-passing); seed
48; full re-order replay certificate max-abs<1e-6.

## The deltas (only these differ from color)

**Δ1 — Axis.** results/illum_polarity_axis_v3_48.npz, projection key **"dark"**
(color used color_salience_axis_48.npz key "axis"). npz keys VERIFIED: mu / W /
dark present (also dark_raw, valence — unused). Loading pattern is
component_batch_demo_52.py's illum branch. sha256 52bfe9c803c41649…

**Δ2 — ¬realized uses the WIDENED WHOLE-FIELD illumination set** (her explicit
ruling: the dark run's realized check uses the SAME widened set as the
illumination boolean, not a bare dark-gloss check). Imported VERBATIM from
marking/tools/illumination_labeler_53.py:
- DARK pole — bare-English token in DARK_GLOSS {black, dark, dim, gloomy};
- BRIGHT pole — FULL PAIR in BRIGHT_PAIRS {bright|明, lights|光, Brightness|明暗,
  illuminate|照射} (full-pair matching load-bearing: bare 光/明/照 would wrongly
  grab polished|光, explain|说明, TakeCare|照料).
A word is realized (open illumination) iff any of its own word-level DEF records
fires either pole (mirrors latent_written_labeler_53's illumination check).
VERIFIED at build: 黑夜 · 明天 · 每天 · 前天 · 昨天 · 后天 are all ¬realized under
this set (their word DEFs are temporal), so each scores latent-if-it-clears. The
ensemble generator's own "charged" definition (bare dark gloss) is left UNCHANGED
— only this scorer-side realized conjunct is widened.

**Δ3 — Pool provenance: NO dark validation pool exists; validation DEFERRED, no
pool invented.** The house record said the dark pool = the R1-gamma latent-tier
item pool for dark/illumination. It does not exist: both r1_gamma_salience_51.json
and r1_gamma_eval_words_51.json carry fields **{color, plant, sound} only** — no
dark/illumination field. Corroborated by results/dark_boolean_calibration_53.json
(outcome STOPPED_AT_STEP_2, "no_dark_field_in_ground_truth"), the boolean side's
same wall. Per the staging law a pool is NOT invented. host_frames_53.json's
latent(37)/control(108) roles are COLOR-provenance (gamma color). Consequence,
declared: the scorer computes the confusion/F1 over those roles but emits it
LABELED "DIAGNOSTIC ONLY — color-provenance roles", and the **verdict is
DEFERRED**, awaiting her dark-pool ruling. This is the exact parallel of the
color run's own referent re-validation deferral (two-row registration, AMENDED:
"the R1-era pool was constructed char-first and cannot test the referent row").
The selftests remain the hard gate (fail = stop) — they are dark-valid known
answers, pool-independent.

**Δ4 — Data addenda** (base files load as for color; both dark addenda added).
- Hosts: results/host_frames_53.json (unchanged; covers the color pool) +
  results/host_frames_dark_addendum_53.json (NEW — the deictic time-word probes
  absent from the base, whole-token Leipzig hosts by the identical procedure,
  1-based line, 0-based positions). sha256 bae8605d208d72c9… Contents:
  前天 (n=24), 昨天 (n=259), 后天 (n=44), 後天 (n=0 → sits out). Leipzig
  sha d0073992… (matches base).
- Ensembles: results/substitution_ensembles_53.json (base, color+dark; the dark
  field for all original words already generated; sha 76af1c1c…) +
  results/substitution_ensembles_53_mingtian_addendum.json (明天, color+dark;
  sha ec678bb9…) + results/substitution_ensembles_53_dark_addendum.json (NEW —
  前天/昨天/后天 dark = fallback n=899 each; 後天 = empty, word_not_in_hownet).
  sha256 50447fd45bc8559c… Generated by closure-copy of
  substitution_ensemble_gen_53.main() (identical to gen_mingtian_addendum_53.py's
  copy), DRIFT-CHECKED: 波黑 color+dark reproduce byte-identically before emission
  (else refuses to emit) — PASSED at build.

**Δ5 — Selftests (dark-era known answers; fail = stop).** From the v1 design
doc's selftest block adapted to the color-run structure:
| word | expectation |
|---|---|
| 波黑 | dark-LATENT (carrier 黑 prints black; n=1 host, FLAGGED-thin) |
| 波兰 | dark-negative (v1: "no dark"; 蘭 not dark-charged) |
| 青春 | latent-candidate per DEF (carrier 青 prints black; word ¬realized widened); call charge-dependent |
| 鲤鱼 | dark-negative (fish; no dark carrier) |
| 乌干达 | dark-LATENT (carrier 乌 prints black\|黑; word ¬realized — place name; n=4, flagged) |
| 竹马 | SIT OUT (0 hosts) |
| 鳕鱼 | SIT OUT (0 hosts) |

**黑夜 MOVED from selftest to PROBE.** In the color scorer 黑夜 was a selftest;
for dark its word DEF is temporal ({time|时间:TimeSect={night|夜}}) and carries
NO dark/bright gloss under the widened set (VERIFIED) — so ¬realized HOLDS and it
scores latent-if-it-clears. It rides as an OPEN probe. (This also resolves a
pre-existing color-scorer inconsistency, noted below.)

**Δ6 — Probes (all OPEN; either outcome a finding) + pre-committed contrast.**
The deictic time-word family, reader marks quoted verbatim from
reader_column_timewords_53.md:
| word | reader mark | note |
|---|---|---|
| 明天 | "very very bright and slightly warm-white" | written sensor fires bright (明) |
| 前天 | "no brightness" | — |
| 昨天 | "dim" | 昨 carries no dark gloss (reader's dim has no print account) |
| 后天 | "no brightness" | reader marked traditional 後天 (absent from HowNet AND Leipzig); 后天's hosts conflate day-after-tomorrow with the {acquired\|后天} sense — DECLARED, not filtered |
| 每天 | "slightly bright" | control-role in base pool |
| 黑夜 | (not in the reader column) | word DEF temporal, ¬realized holds |

**後天/后天 form check (as required):** HowNet presence — 后天 YES, 後天 NO;
Leipzig whole-token hosts — 后天 YES (44), 後天 NONE (0). Only the simplified
后天 is scorable; the reader's traditional 後天 has neither a host nor a lexicon
entry and sits out. The probe rides on 后天 with the sense-conflation flag.

**PRE-COMMITTED CONTRAST:** charge(明天) − charge(前天), the pair controlling 天,
computed and reported as its own line. Expectation OPEN (the reader's localized
structure — 明天 bright, 前天 nothing — is hers to break against the axis).

## What this registration does NOT do
No change to the shared mechanism, floors, null design, cap, seed, or
certificate. No new axis (the illum v3 axis is credentialed already). No pool
invented. No re-scoring of the color run.

## Noted, not fixed (reported per house law)
The shipped color scorer (word_latent_v2_incontext_53.py) carries 黑夜 as a
selftest expecting realized=True, while its own registration (§Selftests,
CORRECTED 07-20) rules 黑夜 print-realized FALSE and OPEN. Under a color-gloss
check 黑夜's word DEF is color-silent, so that selftest would FAIL (fire the
fail=stop) if the color run were executed. Not this run's file to fix; the dark
run classifies 黑夜 correctly as an OPEN probe (Δ5). Flagged for her.

## Outputs
results/word_latent_v2_incontext_dark_53.json (per-item table + color-roles
DIAGNOSTIC confusion + selftest/probe/contrast blocks + provenance) and a summary
MD beside it. **RUN ONLY ON HER GO.**
