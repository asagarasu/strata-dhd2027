# Word-latent v5 REFERENT-COLOR — registration (#54, 2026-07-22; BROKEN at her word, 00:00-7min Shanghai: "Color: agree, go for it.")
*Attempt-5 = attempt-4 with the EXAM REPAIRED, nothing else. Abort #8
stands as published (her ruling: banana stays in the score — declared
means named-in-print, never held out). Every change below is
prospective, evidence-backed by the committed diagnostic + the 同一
candidate-grain exhibit (results/fp_cross_axis_diagnostic_54.md), and
none touches the mechanism, floors, pool positives, or truth column.*

## Unchanged from the broken v4 (not re-argued)
Positives = the frozen six (banana IN, her ruling verbatim). Truth =
norms (CCFD floor .20 + strength rule A). Mechanism, K=20, cap 32,
seed 48, z ≥ 1.5, F1 ≥ .70, null symmetry, caption law, drift gate,
selftests, abort-safe outputs.

## The two repairs (both = her findings, operationalized)
1. **CONTROL-VALIDITY LAW** (her unfair-exam ruling): an item whose
   expression cannot be swapped cannot serve in the null. Mechanical
   criterion, declared: a control is INVALID if (a) it is a 4-char
   idiom (成语 list membership — citable lexicon; jieba's idiom
   resource or the MOE 成語 marker, source recorded in the run), OR
   (b) fewer than MIN_NAT of its ensemble candidates clear the
   attestation floor below. Invalid controls are LISTED with reasons,
   never silently dropped. (言之凿凿 exits by (a) and by n=2-thin;
   同一 is handled by repair 2, not exclusion.)
2. **ENSEMBLE ATTESTATION FLOOR** (the 同一 exhibit): a substitution
   candidate is ADMITTED only if it is attested as a whole token
   ≥ F_MIN times across the host corpora (Leipzig tokenized ∪
   captions — the same corpora that supply hosts; counts published
   per candidate). Proposed F_MIN = 5, sensitivity table at 3/5/10
   published with the run. Words whose admitted ensemble falls below
   MIN_NAT = 3 candidates sit out with the reason (positives:
   frozen-pool violation check unchanged). Evidence: 同一's natural
   swaps (同样/相同/同等 — all corpus-frequent) carry ≈0 charge; its
   charge lived entirely in unattested/rare siblings (等压/趋近/俨若).
   The repair removes the awkward-sibling artifact for EVERY word
   symmetrically — positives included — so it cannot be a thumb on
   the scale: 桔子-class ensembles are fruit-swaps and survive intact.

## Predicted-and-published expectations (so the run can embarrass us)
同一 decharges (its admitted ensemble = the natural swaps that read
≈0); 言之凿凿 exits as invalid control; the six positives' charges
move little (their ensembles were already natural). If instead a
positive's charge collapses under attestation, that is a FINDING
against the meter and publishes as such.

## What this registration refuses to do
No retroactive rescoring of attempt-4 (abort #8 is the record); no
residual hold-outs (her cheating ruling); no cross-axis
residualization (the diagnostic killed it — TPs load semantically-
adjacent axes and would be destroyed); no new axes, no new truth.

⚑ HER BREAKING, then one go. Registration commit precedes the run.
