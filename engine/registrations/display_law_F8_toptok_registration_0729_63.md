# REGISTRATION — display-law amendment F8: salience top-tok goes positive-only
*#63, 2026-07-29 sitting. Her ruling, this sitting: "top-tok for salience
axis should not be absolute values, or else I am reading bad figures" —
a remaining bug from the bidirectional-for-all era. Staked before
implementation; the implementing run cites this file.*

## The bug
Gate C selects top-tok as the |Δ|-max contentful token — a bidirectional-
era convention. Since v5.0 the trigger law admits POSITIVE movements only
on the salience axes (colour · sound · plant), and F4 already draws the
cut-dash one-sided there. Top-tok never got the matching amendment, so
exhibits highlight tokens the trigger law rules inadmissible. Empirical
bill, one sitting (07-29): three misreadings by the lead — owen "shuttle."
−0.0308 over clacking +0.0249 (fired) · forke "Weberin" −0.0466 read as a
hard fire (zero admissible fires) · guo "落地" −0.0250 over 一旦 +0.0226
(fired).

## The amendment
- SALIENCE axes (colour, sound, plant): top-tok = max POSITIVE Δ
  contentful token. If no positive contentful token exists, the row draws
  the LEAST-NEGATIVE contentful token in FADED ink with its signed value
  (her amendment, this sitting: the reader deserves to see what came
  nearest; the third display grammar — full · italic · faded — already
  exists per [3.7], so nearness shows without wearing a fire's ink).
  Never full-ink fallback to |Δ|.
- VALUE axes (illumination, temporal duration): |Δ|-max stands (two-sided
  by law).
- Gate C's assertion and the verify_exhibits_60 mirror amend in LOCKSTEP
  (two locks, one law). Gate E (highlight IS the top-tok) unchanged.

## Scope + sequencing
- Implementation: exhibit_gen_60 top-tok selection + gate C + the mirror;
  then regenerate the two current-law panels
  (albatros_L15_color_h1_exemplar_63.svg · loom_exhibit_tiaotiao_L4_
  sound_63.svg) and any exhibit emitted after this date. KEY reading
  guide's top-tok wording reviewed at the same pass.
- One writer: implementation waits until the in-flight loom regeneration
  lands (the same machinery is in use); that panel arrives with the old
  highlight and is redrawn immediately after.
- NOT submission-blocking: none of the three submission figures (figure 1
  · figure 2 grid · credential table) carries a top-tok strip.
- Census untouched: this is display law only; triggered_tokens and all
  census tiers are unaffected (they were already positive-only at v5.0).
