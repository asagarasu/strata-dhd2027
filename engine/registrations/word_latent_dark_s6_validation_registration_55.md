# Word-latent DARK — S6 VALIDATION registration (#55, 2026-07-22) — **BROKEN at her word (18:18 Shanghai, in-session: "Decile then")**

*Her breaking followed the chair's in-session evidence on her 深/沉/浓 pause
(the ejected cluster's probe readings: one knife-edge flicker 深 +1.52, the
rest null/bright-lean; three channels convergent). Recorded with the
breaking: (i) the ejected cluster files to the known-uncaptured register as
DEPTH-METAPHOR DARKNESS, 深's flicker a lead for the MOE phrasing-widening
door (L1 gap, v4 registration); (ii) the scene witness carries a declared
limit — PHOTOGRAPHY NEEDS LIGHT: Places365 medians under-darken the darkest
referents (underwater rank 47; 牢房 misses the decile by one rank) — a
trigger-channel bias now demonstrated, not suspected. The FA formula was
already hers (e1 breaking, 18:14). Verdict fired same sitting: `--go`.*
*e2 of the ruled queue (40160a7), UNSHELVED by your two rulings: the direction
contract (S6) + the vision conjunction ("an illumination-triggered word with
strong visual impressions is truth-supported") jointly created dark's truth
tier. Your TOP-36 scene-cut word (07-22 16:00, decile) re-cuts the tier;
T1/T2 unaffected. This registration gives the dark row its credential-claim
shape before 08-01. One page + the frozen list.*

## Positives (FROZEN, n = 56) — the strength-covered tier re-cut at TOP-36, 夜景 in it
The `covered_strength_only` tier of `results/dark_pool_assembly_v2_PROPOSED_54.json`
(84 members under the darkest-QUINTILE T3), re-derived under your DECILE word:
a T3-scene member stays iff ≥1 of its triggering categories ranks in the
darkest 36 (witness `candidate_cuts.darkest_decile`, median ≤ 98.432);
T1-addendum/T2/witness triggers unaffected. 84 → **56**:

- **T3 kept (10):** 余地 台 响 地方 执行 演出 空间 表演 表现 餐厅
- **T3 cut (28):** 中心 公路 内部 厚 园林 机舱 沉 法院 浓 深 深刻 深沉 瀑布
  火山 牢房 球场 甜 细胞 花园 行 购物 路途 追求 道 道路 重 阁楼 香
  *(the /u/underwater metaphor cluster exits wholesale — your decile instinct's
  visible effect)*
- **Non-T3 strength, unaffected (46):** 下降 乾 云 传 冬天 冷 出去 前景 商量
  **夜景** 大 女神 学校 小人 彷徨 打 放 早 星空 月 柔情 格调 求婚 洋洋 流星
  浩浩 消失 清淡 清脆 漠漠 炎炎 烈 玫瑰 理论 盈盈 相同 研究 私奔 美 脆 著名
  说 调和 走路 雪花 默契

Truth citations per member = the v2 assembly record VERBATIM (zh3000/zhong2022
strength values, sha-pinned) under your vision-conjunction ruling. **No
strength floor is proposed** — the tier as your rulings made it; a floor is
yours to set at breaking if "strong" wants a number.

## Contract (S6, the direction ruling) + the FA law (the sound re-registration's formula)
- **FLOOR = precision-against-truth**: fires among VALID controls bounded by
  **FA_BOUND(n_valid) = min{k : BinomCDF(k; n_valid, p₀) ≥ .95}, p₀ = 1−Φ(1.5)
  = .066807** — the null's own 95th-percentile count (exact binomial; the law
  proposed in `word_latent_sound_referent_reregistration_55.md`, one breaking
  covers both or neither).
- **RECALL over the attested field = ALIVENESS-RATE, unfloored** —
  characterization, never an abort. F1 reported continuity-only.
- **Controls** = the attempt-4 law minus your five probes (101); **validity =
  the v5 exam-repair law** (4-char idiom ∪ attestation-starved leave the null,
  LISTED never dropped) — the probe run predates that law and scored e.g.
  深入人心/吃得下; under this registration they exit the null.

## Charges = the committed probe record, verdicts re-derived (no re-embedding)
The v4 probe run (BROKEN at your word 07-22, fork (a)) already embedded this
pool on the dark axis (`illum_polarity_axis_v3_48.npz` key `dark`, K=20 cap 32
seed 48, certificate 0.00e+00, 179,932 texts) — the machinery is deterministic,
so a re-run reproduces identical charges. This registration therefore takes
the committed per-word CHARGES verbatim (sha-pinned record) and re-derives
under its own law: null membership (v5 validity) → null mean/sd → z → fires →
FA verdict. Marginal z's may move with the null; they publish wherever they
land. **A fresh one-go re-run is offered at your word; determinism makes it
reproduction, not evidence.**

## Sequence disclosure (the honesty paragraph)
The probe outcomes were committed and public BEFORE your TOP-36 word. The cut
criterion is luminance-only and was picked among the scene witness's three
PRE-PROPOSED cuts (decile/quintile/valley — "no invented threshold; your
pick"); it never saw a charge. Expected outcome under the probe's own null,
stated openly: positives 5 fired (夜景 +14.06 · 冬天 +5.69 · 月 +3.04 · 冷
+2.81 · 清淡 +1.63) / 50 silent / 1 sit-out (浩浩) → aliveness ≈ .09; control
fires 晚餐 +2.94 (valid) + 深入人心 +1.55 (exits as idiom) → expected fp ≈ 1
vs FA_BOUND ≈ 7–8 at n_valid ≈ 60 → **precision floor expected to HOLD; the
low aliveness-rate is the FINDING** (the strength tier is impression-truth;
most of it reads text-silent on the dark axis — the 山楂/dormant shape at
field scale, if it holds).

## What this registration refuses to do
No new triggers, axes, or truth; no hand-pruning beyond your ruled cut; no
rescoring of committed charges; no strength floor invented by the chair; the
probe's exploratory/probe outcomes stay its own record, untouched here.

⚑ **YOUR BREAKING** (formula + frozen 56 + validity law), then one go:
`word_latent_dark_s6_verdict_55.py` computes the verdict from the committed
records and publishes whichever way. Registration commit precedes it.

---

## REPAIR (registered 2026-07-23, review-mandated) — parse the CANONICAL JSON, not the rounded Markdown

*This block is APPENDED (the original registration above is untouched) and is
committed BEFORE the corrected record is produced, per the register-then-execute
discipline. It repairs a parser defect the external review found; it changes NO
law of this registration — the null membership (v5 validity), FA_BOUND, z-floor,
frozen 56, and PASS rule are exactly as broken above. Only the CHARGE SOURCE is
corrected: full-precision JSON per-word records replace the 4-decimal Markdown
table the verdict script had been reading.*

**The mandate (Codex external adversarial review 2026-07-22, Finding 7, VERBATIM):**

> ### 7. e2 computes from rounded Markdown, changing a false-positive call and inventing a record gap
>
> **Defect:** The verdict script uses a human presentation table as data instead
> of the canonical JSON. Four-decimal rounding changes one threshold call, and
> omitted Markdown sit-outs make an existing JSON record look absent.
>
> - **Citation:** `parse_probe_table` reads Markdown charges as "VERBATIM"
>   (`caesitas_proto/word_latent_dark_s6_verdict_55.py:58-77`), but the table writer
>   rounds them to four decimals and prints only scored items
>   (`caesitas_proto/word_latent_v4_dark_proberun_54.py:410-422`). The later code
>   labels absent rows as record gaps
>   (`caesitas_proto/word_latent_dark_s6_verdict_55.py:90-104,145-151`). Yet the
>   canonical probe JSON records 浩浩 with nine hosts, `status=sit_out`, and
>   `reason=word_not_in_hownet`
>   (`caesitas_proto/results/word_latent_v4_dark_proberun_54.json:12541-12552`).
> - **Claim at risk:** "**Charges = the committed probe record verbatim**," "**fp =
>   1: 晚餐**," and 浩浩's "absent_from_committed_record" diagnosis
>   (`caesitas_proto/results/word_latent_dark_s6_verdict_55.md:3-10,51`).
> - **Verification:** Recomputing the registered 57-control null from JSON gives
>   mean `0.0002256531700`, population SD `0.0052558188385`, and two calls:
>   `晚餐 z=3.02684` and `生活状况 z=1.50665`. Thus FP is 2, not 1; corrected F1 is
>   about `.161`, not `.164`. The nominal S6 verdict still holds (`2≤7`), and
>   aliveness remains 5/55 conditional on scorable positives.
> - **Severity:** **WEAKENS-CLAIM**. It does not rescue or overturn dark's nominal
>   PASS, but the published precision, FP list, and provenance narrative are wrong.
> - **Settle it:** Parse `probe_json["items"]` with full-precision charges and
>   canonical statuses/reasons; assert that every frozen positive is accounted for;
>   register the repair; and publish a corrected verdict with FP=2 and 浩浩's real
>   sit-out reason.

**The repair (exactly the settle-it, nothing more):**
- The corrected verdict script `word_latent_dark_s6_verdict_55_corrected.py`
  parses `probe_json["items"]` with **full-precision** charges and the
  **canonical** per-word `status`/`reason`. `parse_probe_table` (the rounded-
  Markdown reader) is retired; the invented `absent_from_committed_record`
  "record gap" branch is removed.
- It ASSERTS that every one of the frozen 56 positives is accounted for in the
  JSON as exactly one of {scored, sit_out}.
- 浩浩 publishes as a REAL sit-out with the canonical reason `word_not_in_hownet`
  (nine hosts, no HowNet ensemble) — not a fabricated record gap.
- Expected under the corrected parse (Codex's recomputation, staked here as the
  acceptance gate; the script HARD-STOPS if it does not reproduce them):
  null n=57 mean `0.0002256531700` sd `0.0052558188385`; **fp = 2**
  (`晚餐 z=3.02684`, `生活状况 z=1.50665`); aliveness 5/55; F1 ≈ `.161`;
  FA_BOUND = 7; **verdict PASS (2 ≤ 7)** — the nominal S6 verdict is UNCHANGED.
- The wrong committed record `results/word_latent_dark_s6_verdict_55.{json,md}`
  STANDS as the erroneous artifact of record; a superseded-by note is appended to
  its `.md`. The corrected record is written to NEW files
  `results/word_latent_dark_s6_verdict_55_corrected.{json,md}`.
