# Word-latent v3 TWO-ROW — registration DRAFT (#53, 2026-07-20 late night; FOR HER BREAKING, RUN ONLY ON HER GO)
*Follows abort #4's three-oracle diagnosis (results/
word_latent_v2_incontext_color_53.md + reader_column_color_53.md)
and her ruling tonight: the latent layer SPLITS — "latent-color-
referent and latent-color-written… keep it separated." Two rows,
two sensors, two credentials. Nothing below runs without her word.*

## The split (her design, the diagnosis's shape)
- **latent-F-WRITTEN** — the field lives in the visible character
  (釋字-level): 赤 in 赤字, 黑 in 抹黑, 明 in 明天. Sensor =
  RULE: char print-charged ∧ word-DEF F-silent ∧ liveness band
  recorded. Citable lookups only; no embedding. zh = HowNet char
  DEFs (marking/tools/latent_written_labeler_53.py); en = Skeat
  etymon chains; grc = LSJ (etym_chains_v1_52.py) — the founding
  line (καλχαίνω → κάλχη) is THIS row, and the mechanisms match
  across languages without forcing.
- **latent-F-REFERENT** — the field lives in the world referent:
  scallions green, tomatoes red, ginkgo silver-gold. Sensor = the
  in-context substitution instrument (v2 mechanism unchanged:
  ensemble-median × host-median deltas vs matched-control null).
  The abort's six hits + her blind confirmation of all six are this
  row's existence proof.
- Translation-survival defaults are OPPOSITE, which is the rubric's
  gain: referent color survives translation automatically; written
  color dies at the script boundary unless COMPENSATED — its
  survival score is a compensation-event detector.

## Credentials (different per row, by the rows-follow-measurements law)
1. **Written sensor**: boolean-family credential — citable
   derivation (one gloss rule, shared with the word-boolean's
   sets), committed selftests (fail = stop), liveness band + edge
   flags emitted, NO statistical floor (a rule is credentialed by
   its citations). Divergences (sensor fires, reader none — the
   東西-class) go to the ledger as rows, never threshold-tuned.
2. **Referent instrument**: F1 ≥ .70 (floor unchanged, fifth
   attempt) — but validated against the REFERENT-LAYER SUBSET:
   positives = pool-latent words the written sensor does NOT claim
   (the partition is mechanical — the rule itself divides the
   pool); negatives = controls, CJK-only (numerals OUT — the "19"
   z=3.22 false alarm and null pollution, published in abort #4,
   are the reason). Mechanism, K=20, cap 32, seed 48: unchanged
   from v2 (results/latent_written_partition_53.json carries the
   partition).

## Pool corrections (each with its published reason)
- Numerals/non-CJK strings dropped (abort #4 false alarm + null
  noise; drop-nothing served its diagnostic purpose and retires).
- 波兰 selftest expectation FLIPPED to referent-positive: reader
  datum "older Chinese still spell 蘭色 for blue" + z=2.60 blind-
  confirmed. (Written sensor stays silent on it — 兰 prints plant —
  which is the split working, not a conflict.)
- 发扬光大 reclassified out of quiet-controls for ILLUMINATION
  (visible 光; her blind mark "bright"); stays control for color.
- 缜密's color-pool positive flagged cross-field (her mark "light
  blocked" = illumination impression); the sitting decides its row.

## Selftests + probes (color; dark run mirrors)
Written row: 赤字 ✓fires(赤) · 抹黑 ✓fires(黑) · 波黑 ✓fires(黑) ·
黑夜 ✓fires(黑, word-home temporal — her home rule, the v2 finding)
· 波兰 ✗silent · 每天 ✗silent · 同一 ✗silent.
Referent row: 波黑 + · 波兰 + (flipped) · 乌干达 + · 鲤鱼 − ·
青春 per-DEF · zero-host sit-outs unchanged (竹马/鳕鱼/绿速达).
**Paired probes for the DARK run (her two time-words):** 明天 —
written FIRES (明 bright-print) + context tier votes + her reader
"very very bright, slightly warm-white"; 每天 — written SILENT
(no bright char) + context tier votes + her reader "slightly
bright". The split's own prediction: if 每天's brightness is real
it can ONLY arrive via the referent tier — the two probes separate
the rows empirically or embarrass the design. Either way a finding.

## AMENDED pre-breaking (07-20 late night, dated — the partition run's fact)
The built sensor's partition over the 154-pool: written-claimed 39
(color) — including 13 of the axis's 14 misses and, on inspection,
the axis's hits too (葱/红/银/乌 are all print-charged chars). Pool-
latent ∧ written-silent = **n=2** (鲜活, 鲜花 — 鲜 prints bright
and colored|彩, neither in the bare color set; their red is
referent). Two positives is numerology by the house's own small-n
law (the jp deferral). Therefore: **the referent re-validation on
COLOR is DEFERRED pending her referent-pool ruling** — the R1-era
pool was constructed char-first and cannot test the referent row.
Standing referent-tier evidence meanwhile: 波兰 (written-silent,
axis z=2.60, reader-confirmed 蘭色) = one confirmed referent
detection; 鲜活/鲜花 = its two known missed targets; the 6 hits =
both-layer words. Candidate referent-pool sources FOR THE SITTING,
listed not chosen: her reader column; COCO-witness colorful-
referent nouns; plant/food lexicon classes. The DARK run's paired
probes (明天/每天) are unaffected and remain the split's live test.

## What this registration does NOT do
No re-scoring of v2's numbers (the abort record stands as
published); no new axes; no panel revival; no change to the
mechanism that went six-for-six. The one new artifact is a rule
sensor in the existing boolean family, at her explicit design.

## Outputs
Written sensor: selftest MD + partition table (built pre-
registration as staged evidence, marked report-not-validation).
Referent re-validation: results/word_latent_v3_referent_color_53.json
+ MD, same record shape as v2 including abort-safe structure.
