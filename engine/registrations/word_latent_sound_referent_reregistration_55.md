# Word-latent REFERENT-SOUND — RE-REGISTRATION (#55, 2026-07-22) — **BROKEN at her word (18:14 Shanghai, in-session: "sound rule fix: agree to the fix")**

**EXECUTED on her breaking:** FA_BOUND law adopted (binomial .95 of the null's
own chance count; 7 at n=59). The committed record re-verdicts as tabled
below: fp = 2 ≤ 7 → **the sound run's verdict of record is now PASS
(precision) — S6; ALIVENESS-RATE .381 (unfloored characterization); F1 .516
continuity-only.** The landed run's files stay untouched per this
registration's own law; this block + §7 + the inventory carry the verdict.
*g2 of the ruled queue (her words folded, 40160a7) executing A.1's open call:
"FA bound calibration OPEN (zero was over-strict; bound vs the null's chance
rate ≈ 6.7% at z≥1.5 — needs her breaking; the sound abort is its exhibit)."
This document states the formula for your breaking. It changes ONE constant's
law and re-verdicts the COMMITTED run record. Nothing re-runs, nothing
rescores, no output byte of the landed run is touched.*

## The one change
The broken v2 registration set **FA_BOUND = 0** (marked PROPOSED, "your
breaking may relax it to a rate"). The run landed 2 fires among 59 valid
controls and ABORTED on that constant — while the null's own chance behavior
at z ≥ 1.5 expects **3.94** fires in 59. Zero rejects runs that behave BETTER
than chance; the floor was measuring the constant, not the meter.

**Proposed law (portable to every S6 registration forward):**

FA_BOUND(n_valid) = the null's own 95th-percentile false-alarm count
= min{ k : BinomCDF(k; n_valid, p₀) ≥ .95 },  p₀ = 1 − Φ(z_floor)

At the registered z_floor 1.5: p₀ = .066807. At the committed run's
n_valid = 59: **FA_BOUND = 7** (CDF: k=6 → .9026 < .95; k=7 → .9582 ≥ .95).

*(The queue's "bound ≈ 8" was a normal approximation made in passing; the
formula governs — exact binomial, value 7 at this n. Your breaking may move
the percentile; .95 is proposed.)*

## Derivation independence (the honesty of the criterion)
p₀ comes from the registered z-floor alone; n from the committed run's
valid-control count. **The observed fp never enters the formula.**
Disclosure, not blindness: the queue commit (40160a7) already published the
expectation "tonight's 2/59 (below chance) likely PASSES" before this
re-registration existed — the criterion's independence carries the honesty.

## Re-verdict of the committed record (sha-pinned, no re-run)
The mechanism is deterministic (certificate 0.00e+00); the bound is a verdict
criterion over committed outputs — a re-run would reproduce the identical
numbers to re-judge them. Applied to
`results/word_latent_sound_referent_54.json` as committed:

| quantity | committed | under FA_BOUND = 7 |
|---|---|---|
| fires among valid controls | fp = 2 (n = 59) | **2 ≤ 7 → precision floor HOLDS** |
| verdict | ABORT (precision floor fired) | **PASS (precision) — S6** |
| aliveness-rate | .381 (8/21) | unchanged — unfloored characterization |
| F1 | .516 | unchanged — continuity-only, not a floor |

Same pool (21), same axis, same everything else — by construction: nothing
else is touched.

## The two fires, named (record, with one observation)
消息人士 (z +1.61) · 蜂拥 (z +2.26) — both barely above the floor, jointly
below the null's expected count. OBSERVATION for your review, not an argument
and not an action: 蜂拥's written form carries 蜂, whose word (蜜蜂) is a
measured positive at z +4.23; if the written channel leaks into control
charges that is a finding about the axis, and it sits quietly here until you
want it.

## What this re-registration refuses to do
No rescoring or re-running of the landed record; no aliveness floor (her
contract: characterization, never an abort); no pool/control/mechanism edits;
no percentile shopping — .95 is stated once, yours to move.

⚑ **YOUR BREAKING** adopts the formula; on your word the sound row re-verdicts
as above and the state lands in §7 + the inventory at the currency pass.
Recorded by #55; the formula then serves e2 (dark S6) at its own n.
