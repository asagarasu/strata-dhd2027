# Word-latent v7 WIDE REFERENT-COLOUR — registration (#54, 2026-07-22; her go on record: "wide version running now")

*ATTEMPT-7 WIDE = the credentialed attempt-6 scorer with ONE change — the
candidate pool is re-derived over a CCFD-WIDE universe. The mechanism, floors,
controls, truth column, attestation floor, drift gate, and selftests are the
attempt-6 base VERBATIM. BUILD + this registration precede the run; the
orchestrator fires the real run after review. No git commit in the build.*

## The widening (the only change)
The candidate universe is now **ALL 1,410 CCFD concepts** (Deng et al. 2021,
Table2 concept column, minus the `概念` header) — not attempt-4's 13
definition-leg stays + small pixel leg. Two triggers sweep this universe, under
the **truth-only law** (CCFD is the answer key and MAY NOT trigger; the norm sets
Zhong/3000 may not trigger; triggers = witness + pixel only):
- **T1** the zh DEFINITION witness (definition_witness_zh_53 definitional prefix
  exec'd for parse_moe / s2t / witness_word — pipeline NOT run) swept over the
  CCFD universe as a declared ADDENDUM. Colour rule = the witness's documented
  any-colour-family substring law with the 顏色-masking. **Judgment call
  (declared): the one x-ref hop IS TAKEN** — it is cheap (already coded in the
  witness's `resolve()`) and can only ADD recall, never a false positive, because
  truth (CCFD ≥ .20) is computed on the concept and is independent of the trigger.
  A T1 hit = `colorful_full` on the FULL resolved 釋義; the gloss-region primary is
  recorded alongside for disclosure.
- **T2** the pixel-witness chromatic-share islands at her n=0.75
  (referent_witness_v3) via HowNet W_E reverse — the v3 pixel leg, verbatim.

Gates per candidate: hosted (MERGED frames — host_frames_53 → dark_addendum →
group_addendum → widened_addendum, first carrier wins — + caption re-extraction;
universe words with no Leipzig frame count Leipzig 0 and record `frames_absent`,
**the corpus is not rescanned**) ∧ ¬realized (HowNet colour gloss) ∧
written-silent (HowNet colour gloss ∪ ADOPTED MOE-53 chars) ∧ non-empty attested
ensemble (admitted ≥ MIN_NAT after the F_MIN attestation floor). Truth per pool
word = CCFD modal colour feature ≥ .20 (her floor) ∨ strength rule A.
`validation_positives = triggered ∧ gated ∧ truth-supported`.

## Everything else — VERBATIM from attempt-6 (not re-argued)
Mechanism (whole-sentence LaBSE; credentialed colour axis; charge = median-host
median-ensemble Δ). Floors **z ≥ 1.5, F1 ≥ .70**. **K=20, cap 32, seed 48.**
Attestation floor **F_MIN=5 / MIN_NAT=3** (whole-token over Leipzig-tokenised ∪
caption membership). Control-validity law (4-char idiom via jieba POS 'i' ∪
attestation-starved leave the null). 波黑 ensemble drift gate. Null symmetry,
caption law, abort-safe outputs. **Controls UNCHANGED** — the attempt-4 control
set (controls.n = 104; the valid-N re-derives at run time). **Selftests
unchanged** (番茄 / 西红柿 / 鲤鱼, 鲤鱼 OPEN per the attempt-6 amendment; 雪 routed to
illumination).

## Predicted-and-published expectations (so the run can embarrass us)
- The **six attempt-6 positives keep their calls** — 斑马 桔子 胡萝卜 苹果 西兰花 香蕉,
  carried into `validation_positives` with their attempt-4 truth citations. (Build
  finding, published: all six re-derive under the wide T1∪T2; **桔子 and 西兰花 —
  absent from the MOE dictionary — are rescued by the T2 pixel leg**, not T1. No
  fiat carry was needed. Reported, not repaired.)
- The **wide pool's 55 NEW CCFD-wide positives are the test** — their charges are
  the new evidence and publish as such (recall against CCFD ≥ .20 / strength A). If
  a NEW positive's charge collapses under attestation, that is a FINDING against
  the meter and publishes as such.
- **番茄 is promoted** from an attempt-6 selftest to a wide validation positive
  (CCFD 是-红色的 0.767); it scores as a positive and its selftest still evaluates by
  name (¬realized) and passes. Published.
- **Floors unchanged; the THIN flag DROPS** — n = 61 ≥ 10 → status RUN (attempt-6
  was THIN at n = 6). Control validity re-derives: before 104 → 61 valid at run
  time (同一 stays valid but decharges to its attested natural swaps; 言之凿凿 exits
  idiom + starved).

## What this registration refuses to do
No change to the mechanism, floors, truth column, controls, attestation floor,
drift gate, or selftests. No hand-prune of the wide net (the gates and CCFD
truth prune, not the hand). No retroactive rescoring of attempt-6. No new axes,
no new truth. CCFD/norms never trigger.

⚑ HER BREAKING, then one go. Registration precedes the run.

## POST-RUN, her direction ruling (07-22 afternoon, verbatim intent; PROSPECTIVE)
"Here we want to attest that 'text-alive ⇒ norms-attested', not
reverse." Abort #9 stands as registered. Going forward the contract
flips to what the meter actually claims: FLOOR = precision-against-
truth (every fire attested) + the false-alarm bound vs controls;
recall over the attested field = the ALIVENESS-RATE, reported
unfloored (a characterization, .459 measured this run). Under her
arrow this run's numbers read: direction-claim PASSED perfectly
(precision 1.000, fp=0, n=61); the residual measured (the missed 33
= ATTESTED-BUT-DORMANT — her 山楂 datum registered verbatim: "does
not appear red to me unless I actually want to trigger my color
sensor. It is primarily sour and cold" — the third modality-ranking
specimen, first with the on-demand structure explicit). Dormant maps
to the comparator's existing LATENT state; translator-awakening =
the standing REVIVAL cell. No new machinery; new vocabulary.
