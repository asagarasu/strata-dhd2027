# Word-latent EN REFERENT-COLOUR — S6 RE-REGISTRATION (#55, 2026-07-22 night) — **BROKEN BY HER CONDITIONAL PRE-BREAK** — ⚠ **RUN ANNULLED 07-23 (external review, Codex F3): the landed run did NOT execute the registered primary-sense repair** (the wrapper overrode pool/counts/whitening paths but the imported scorer rebuilt ensembles through the old assembly module — banana still carried date/dock/jack, n_admitted 38 not 27; valid controls 78 not 89). **The chair's design-match attestation below was WRONG. Per this registration's own law — "any deviation = it waits for her" — the result is VOID and the rerun waits on her word** (fix: inject the v2 assembly + preflight assertions banana=27/apple=6/polysemes-absent, register the repair, full rerun).

*Her condition, verbatim from the sitting: the registration must match exactly
what was converged — **(a) en-whitening refit + (b) primary-sense + case-exact
attestation, S6 floors, fallback intact; any deviation = it waits for her.**
Chair's attestation: every change below is one of those four items or a
sha-pinned consequence of them; the one geometry question that arose (axis
re-derivation in the new space) was resolved BY NOT DOING IT — it was not
ruled, so doing it would deviate; it is disclosed below instead. e3 of the
ruled queue (40160a7); g3 rulings folded (sisters-repair + en-refit +
NO-SHIP). Registration commit precedes the run; one go, overnight,
abort-safe.*

## The changes (exactly four, all her-ruled tonight)
1. **(a) EN whitening refit** — `results/en_whitening_55.npz` (mu/W), built by
   the committed zh law found and mirrored verbatim (illum_polarity_axis_48.py
   :69-72, symmetric ZCA, eps 1e-5; fit-once-copy law confirmed byte-equal
   across the shelf), N=6000 seeded-48 from vendored leipzig_en,
   never-task exclusion (301 banned words; 271,694 sentences excluded,
   declared). **Verification gate MET: core-families battery 1.000 before AND
   after (≥.95 required); full roster .746→.788 report-only.** Build record:
   `results/en_whitening_build_55.md`.
2. **(b1) PRIMARY-SENSE filter** — a sister lemma is admitted only if the
   co-hyponym synset through which it qualifies is that lemma's WordNet noun
   sense #1 (citable: WordNet sense ordering). Kills date/jack/dock/bugle.
3. **(b2) CASE-EXACT attestation** — `results/en_leipzig_token_counts_exact_55
   .json`; the committed counts were case-folded (benny 77 = Benny 76 +
   benny 1; alexander lowercase = 0 in 19.9M tokens) — a transplant artifact
   from caseless zh, repaired. Kills the name-polluters at the existing
   F_MIN floor. The two repairs are independently load-bearing (exhibits in
   `results/en_pool_v2_PROPOSED_55.md`); basil legitimately survives both.
4. **Pool = the v2 assembly** (`results/en_pool_v2_PROPOSED_55.json`,
   sha-pinned): VP 140→116; 25 words exit by honest starvation (their
   sisterhoods collapse under clean laws — zebra/robin/pea-class, listed);
   all five exhibits stay VP with clean ensembles (banana 47→27, apple 13→6).

## Unchanged (byte-carried; not re-argued)
Mechanism, K=20, cap 32, seed 48, z ≥ 1.5, F_MIN=5, MIN_NAT=3,
control-validity law (validity re-derives under the exact counts at run
time), host frames (_54 — no new words entered), truth column (Buchanan ≥
.20 law), selftests (banana/tomato/table verbatim incl. the banana-class
cross-lingual prediction), certificate, abort-safe outputs. **The axis
vector and projection stay the committed `color_salience_axis_48.npz
["axis"]`** — plumbing note: the runner composes a declared npz
(`results/en_axis_with_en_whitening_55.npz` = en mu/W + committed axis,
3-line construction, sha-pinned in the run record) because whitening and
axis travel in one file by house convention.

## S6 contract (her broken laws, applied)
FLOOR = precision-against-truth: **FA_BOUND(n_valid) = binomial .95 of the
null's own chance count** (her breaking 18:14) at run-time n_valid ·
RECALL over the attested field = ALIVENESS-RATE, unfloored · F1
continuity-only. Strict + wide confusions both re-derived, as the committed
record had them.

## Declared residuals (disclosed, out of tonight's ruled scope; her fallback stands)
- **victorian**-class: admitted purely via case-FOLDED COCO caption
  membership (leipzig-exact 0) — the caption leg's fold not in tonight's
  ruling; surfaced, not absorbed.
- **premier/glad**-class: POS-blind homograph attestation (an adjective's
  tokens attesting a rare noun lemma) — inherent to the byte-carried
  attestation law; a POS gate is a future her-ruling if wanted.
- **Mixed-space geometry**: en whitening under a zh-derived axis direction —
  empirical warrant = the battery gate at 1.000; if the run stays weak, the
  axis (and en register) are the remaining suspects, measured for free.
- Her fallback stays armed: "if still polluted, sisters OUT — same-hypernym
  single-sense lemmas only." Invoking it is hers, on this run's read.

## Expectations (published so the run can embarrass us)
The aborted run under S6 re-read: precision floor would have HELD (fp 7 ≤
bound 10), aliveness .250 strict — the meter was quiet, not hallucinating.
The repairs remove noise-injection (ensembles) and fix the space
(whitening): aliveness is expected to MOVE UP from .250 toward the zh
neighborhood (.459) IF (a)/(b) were the causes; if it does not, that
residual IS the finding (en register / axis — the third suspect, isolated).
No number is promised; the direction and the isolation logic are.

## What this re-registration refuses to do
No axis re-derivation (not ruled); no strict/wide redefinition; no tuning
of any floor, filter, or sample; no rescoring of the _54 record (it stands
as the abort it was); NO-SHIP law untouched — this run's verdict decides
review-readiness, not shipping.

**Run fires on commit of this registration, per her conditional pre-break.
The record of her condition and this attestation IS this header.**
`word_latent_en_referent_color_v2_55.py` — one go; outputs `results/
word_latent_en_referent_color_v2_55.{json,md}`; aborts publish.

## REPAIR REGISTRATION (07-23, her go in-session: "e3 rerun: now? Seems ok to me.")
The wiring fix, registered before the rerun: the runner injects the v2
assembly into the module system BEFORE importing the scorer
(`sys.modules['en_pool_assembly_54'] = en_pool_assembly_v2_55`), so every
internal builder call resolves to the registered repairs — the mechanism
review F3's own read-only check already verified produces banana 27 /
apple 6 / 78 valid controls / bound 9. Assertions, fail = ABORT-record
with no verdict: PRE — injection took; the v2 pool json reads banana 27,
apple 6. POST (on the run's own output): banana n_admitted == 27; no
banned polyseme (date/jack/dock/bugle/benny/alexander) in ANY admitted
ensemble; null n_control == 78. Outputs go to NEW files
(`…_v3_55.{json,md}`) — the annulled v2 record stands untouched as the
record of what ran. Everything else byte-per the registration above.
The verdict prints per the registered contract; its LANGUAGE remains
subject to the open revision (amendment 07-23 block, item 4).
