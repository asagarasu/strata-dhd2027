# CU DISCRIMINATION RUN — the confirmatory reserve, spent (#55.1, 2026-07-23, her words in-session)

*Her three words: (1) disputed-words-as-hard-negatives — "ok"; (2) endpoint
delegated to the chair — chosen below; (3) reserve explained — this run is
the first roast's R6 sealed exam, fixed here, fired once, result stands.
Answers review round 2's central findings (Codex #1/#2): the exam finally
contains the words that could catch the meter lying.*

## Her standing framing correction, RE-RECORDED so it survives sessions
**The scalar marks colour-CHARGEDNESS (salience) — whether colour is alive
around a word — never a specific hue.** (Her correction, made repeatedly;
falls out of session memory; now in the registration layer.) Consequence
honored below: the sensitivity column under sum-of-colour-features.

## Population (colour; zh; all from the committed wide-pool record, frozen #54)
- **DISPUTED (hard negatives): the 43 covered-unsupported words** — CCFD
  covers them (204 raters described them) and no single colour feature
  reaches her .20 floor (modal < .20 ⟹ every colour feature < .20).
  Nominated by the pipeline's OWN committed triggers. **Trigger-quality
  disclosure (her catch, 07-23): for at least 剪刀/帽子 the definition
  proper contains NO colour (`gloss_colorful: false`); the loose full-entry
  substring layer fired (red-family / green-family incidentals). The
  disputed set is therefore the pipeline's noisiest nominations — exactly
  the population review #1 demanded the exam contain.** Scorable subset
  derives at build under the UNCHANGED committed laws (hosting, ¬realized,
  F_MIN=5, MIN_NAT=3); sit-outs listed with reasons, never dropped.
- **SUPPORTED (positives): the committed v7-wide validation set** — their
  charges are the committed record, REUSED verbatim (no rescoring; the
  cross-run null reproduction at 3.79e-19 is the standing warrant that
  re-measurement is reproduction).
- **CONTROLS: the committed control set, scored IN-RUN per the unchanged
  null law** (no shortcuts tonight of all nights — the F3 lesson).
- Machinery: the committed colour meter verbatim (substitution protocol,
  colour-salience axis, K=20, cap 32, seed 48, z ≥ 1.5, certificate).

## Endpoints (fixed before the run; one go; no retries)
- **PRIMARY (pass/fail):** fire-rate(supported) − fire-rate(disputed),
  **one-sided 95% score-interval lower bound > 0** (Newcombe). In words:
  on a pessimistic read, supported words must fire detectably more often
  than the norm-disputed ones.
- **SECONDARY (report-only, with intervals):** charge AUC
  (supported vs disputed, seeded-48 bootstrap) · PPV at nomination
  prevalence (n_supported : n_disputed as nominated, scorable-subset
  disclosed) · the sensitivity column: disputed-set membership under
  SUM-of-colour-class-features ≥ .20 (her salience framing) — reported,
  never switched mid-exam.
- **Conservative counting declared (her 山楂 caveat, against ourselves):**
  production-absence is not association-absence; every disputed-word fire
  nonetheless counts against the meter.

## What passing and failing mean (fixed now)
PASS → the paper's core sentence exists: *the meter discriminates
norm-supported from norm-disputed nominations*, with PPV-style numbers at
declared prevalence; unknowns stay unknowns. FAIL → the latent-referent
colour claim is retired for DHd2027 (rubric + organs + descriptive
demonstration remain), decided tonight with eyes open. **Scope: zh colour
only.** Sound's analog (covered-True subset of its CU tier) is a possible
second run AT HER WORD after this lands; dark is structurally excluded (no
norm base) and stays exploratory regardless.

## Reserve declaration
This is the R6 confirmatory spend (external_review_202607 remediation,
accepted 07-18, never executed): untouched material (the 43 were assembled
#54, never scored), rules fixed in this registration, one execution,
result stands whichever way. No further confirmatory claim rides on
already-explored material.

Registration commit precedes the run. Outputs:
`cu_discrimination_55.py` + `results/cu_discrimination_55.{json,md}`.
Aborts publish. — #55.1, at her "go for the test."

## CHAIR'S PREDICTION (appended mid-run, BEFORE any number exists — verified: no results file on disk; the run is in its build/embedding phase)
The chair stakes the following, in writing, at her challenge to
participate rather than clerk: **predicted disputed fires 2–5 of ~36
scorable** (likeliest members the collocational-leakage class — 帽子 via
红帽子-frequency, 头-class via 黑头发-collocations); **predicted verdict
PASS with a lower bound ≥ +.10**. The failure mode that would surprise
the chair is broad collocational leakage (≥10 disputed fires). If this
run FAILS, the chair's model of the meter was wrong — not merely the
meter. Chair's probability on PASS: ~0.8. — #55.1, committed pre-result.
