# exploratory-unattributed-signal-fields

*Created #56, 2026-07-23 evening, at her word ("We are missing the
unattributed signal folder, Claude") — the row's own home, following
the `<tier>-<row>` folder law (tier: EXPLORATORY). The outputs were
briefly filed inside deterministic-latent-written-fields/ (the same
folded-passenger pattern that had befallen the referent row); moved
here same day via git mv, history intact; relocation noted as a dated
amendment in the registration.*

## The row
**UNATTRIBUTED SIGNAL (reflective-suspect)** — her row, defined and
named 07-23: per line, per field ∈ {colour, sound}, membership =
**channel escape** — the field scalar reads positive (the committed
check-1 sign convention; threshold refinement hers, open) while ALL
three attribution channels are silent: descriptive boolean existed and
did not fire ∧ no written-carrier fire ∧ no referent-witness trigger.
The mechanical population of the reflective-candidate column (adopted
07-22; hand-sourced members 默契/monarch/jet-class — correctly NOT
captured here: their scalars are negative, which is the column's
text-silent signature).

**EXPLORATORY — no credential claims; membership = channel-escape,
not a verdict.** Suspect words per line = the committed top-3 |Δ|
receipts, reported as suspects only. F9: local-tier rows carry no
line text.

## Contents
`unattributed_signal_<board>_56.json` × 9 boards + `_ALL_56.json`
rollup — 818 rows (334 colour · 484 sound). Extractor:
`../unattributed_signal_rows_56.py`; registration:
`caesitas_proto/registrations/unattributed_signal_registration_56.md` (definition
leg-by-leg, shas, examples).

## CURRENT LAYER = _59 (2026-07-27, ghost law)
Read `*_59.json` files only; `*_56*` are the pre-ghost-law layer (superseded-beside).
Law: ../../SCORING_MANUAL_0726_59.md (+GHOST BLOCK). Map: ../../CAESITAS_START_HERE_DRAFT_59.md
