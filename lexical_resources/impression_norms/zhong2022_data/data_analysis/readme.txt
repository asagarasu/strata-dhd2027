There are three types of files:

with suffix of 'Rmd': the R code written in the mark down language
with suffix of 'html': the output file after running all the codes with Knit, all the results are displayed 
with suffix of 'csv': the source datasets to load 

with all these files included under the same directory, you could run the code directly and see the output as domonstrated in the html file.